export interface IWebBasicInfo {
	url: string;
	title: string;
}

export interface IListBasicInfo {
	id: string;
	title: string;
    imageUrl: string;
    parentUrl: string;
}

export interface IFieldBasicInfo {
	id: string;
	title: string;
}